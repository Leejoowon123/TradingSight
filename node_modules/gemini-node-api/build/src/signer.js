import { createHmac } from "node:crypto";
export function SignRequest({ key, secret, payload }) {
  const signature = createHmac("sha384", secret).update(payload).digest("hex");
  return {
    "X-GEMINI-PAYLOAD": payload,
    "X-GEMINI-SIGNATURE": signature,
    "X-GEMINI-APIKEY": key,
  };
}
